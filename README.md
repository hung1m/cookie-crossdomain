Testing access cookies cross sub-domain and parent domain

1. install Node.js
2. set fake subdomain and parent domain in `/etc/hosts` (look at hosts-add file)
3. go into each folder and run: 
    * `npm install`
    * `npm start`